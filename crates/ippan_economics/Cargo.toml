[package]
name = "ippan_economics"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Deterministic round-based emission and fair distribution for IPPAN BlockDAG"
repository = "https://github.com/dmrl789/IPPAN"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

[dev-dependencies]
serde_json = "1.0"
