# Use crates.io by default. To enable offline vendoring, run `cargo vendor`
# and configure the vendored sources explicitly in CI.
[source.crates-io]
registry = "https://github.com/rust-lang/crates.io-index"
