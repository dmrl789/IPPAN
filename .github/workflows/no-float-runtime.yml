name: 🧮 No Floats in Runtime

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

permissions:
  contents: read

jobs:
  test-no-float-runtime:
    name: Ensure runtime crates have no f32/f64
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ripgrep
        run: |
          if ! command -v rg >/dev/null 2>&1; then
            sudo apt-get update
            sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends ripgrep
          fi

      - name: Check for forbidden floats in runtime crates
        run: |
          set -euo pipefail
          echo "Checking for f32/f64 in runtime crates (.rs only, excluding tests/examples/benches)..."

          paths=(
            crates/ai_core/src
            crates/consensus/src
            crates/consensus_dlc/src
            crates/ai_registry/src
          )

          matches=$(rg \
              --glob '*.rs' \
              --glob '!**/tests/**' \
              --glob '!**/examples/**' \
              --glob '!**/benches/**' \
              -n \
              '\b(f32|f64)\b' \
              "${paths[@]}" || true)

          matches=$(echo "$matches" | grep -Ev '^\s*//' || true)

          if [ -n "$matches" ]; then
            echo "❌ Found forbidden float usage in runtime crates:"
            echo "$matches"
            exit 1
          else
            echo "✅ No forbidden f32/f64 usage detected in runtime crates."
          fi
