# IPPAN Unified UI Configuration
# Copy this file to .env.local for local development
# Copy this file to .env.production for production builds
# DO NOT commit .env.local or .env.production to version control!

# ============================================================================
# IMPORTANT: All NEXT_PUBLIC_* variables are exposed to the browser
# DO NOT put secrets in NEXT_PUBLIC_* variables!
# ============================================================================

# ============================================================================
# UI Feature Flags
# ============================================================================

# Enable full UI features (set to 0 to disable)
NEXT_PUBLIC_ENABLE_FULL_UI=1

# Enable AI features (set to 0 to disable)
NEXT_PUBLIC_AI_ENABLED=1

# ============================================================================
# Network Configuration
# ============================================================================

# Network display name shown in the UI
NEXT_PUBLIC_NETWORK_NAME=IPPAN-Devnet

# ============================================================================
# API Endpoints (REQUIRED)
# ============================================================================

# Gateway API URL (for API requests)
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8081/api

# Base API URL (direct node access)
NEXT_PUBLIC_API_BASE_URL=http://localhost:7080

# WebSocket URL (for real-time updates)
NEXT_PUBLIC_WS_URL=ws://localhost:7080/ws

# ============================================================================
# CDN & Asset Configuration (Optional)
# ============================================================================

# Asset prefix for CDN or subdirectory deployment
# Leave empty for root deployment
# Example: /static or https://cdn.example.com
NEXT_PUBLIC_ASSET_PREFIX=

# ============================================================================
# Server-Side Configuration
# ============================================================================

# Node environment
NODE_ENV=development  # Options: development, production

# Server port (for standalone mode)
PORT=3001

# Server-side full UI toggle (redundant with NEXT_PUBLIC_ENABLE_FULL_UI)
ENABLE_FULL_UI=1

# ============================================================================
# Production Example
# ============================================================================

# For production, update endpoints to your production URLs:
# NEXT_PUBLIC_GATEWAY_URL=https://api.ippan.network/api
# NEXT_PUBLIC_API_BASE_URL=https://node.ippan.network
# NEXT_PUBLIC_WS_URL=wss://node.ippan.network/ws
# NEXT_PUBLIC_NETWORK_NAME=IPPAN Mainnet
# NODE_ENV=production

# ============================================================================
# Local Development Example
# ============================================================================

# For local development with docker-compose:
# NEXT_PUBLIC_GATEWAY_URL=http://localhost:7080/api
# NEXT_PUBLIC_API_BASE_URL=http://localhost:7080
# NEXT_PUBLIC_WS_URL=ws://localhost:7080/ws
# NEXT_PUBLIC_NETWORK_NAME=IPPAN-Local
# NODE_ENV=development

# ============================================================================
# Notes
# ============================================================================

# 1. NEXT_PUBLIC_* variables are embedded in the browser bundle at BUILD time
# 2. For dynamic runtime configuration, use getServerSideProps or API routes
# 3. Never expose API keys or secrets in NEXT_PUBLIC_* variables
# 4. WebSocket URLs must use ws:// or wss:// protocol
# 5. Ensure CORS is configured on the backend for your domain
