# Unified UI environment configuration for production builds
#
# Copy this file to `.env.production` (or `.env.local` for local overrides)
# and adjust the values to point at the RPC endpoint and websocket gateway
# available to end users.

# Browser-exposed settings (compatible with both Vite and Next.js style prefixes)
NEXT_PUBLIC_API_BASE_URL=https://api.ippan.org
NEXT_PUBLIC_WS_URL=wss://api.ippan.org/ws
NEXT_PUBLIC_NETWORK_NAME=IPPAN-Devnet
NEXT_PUBLIC_EXPLORER_BASE=https://explorer.ippan.org

# Vite legacy compatibility (mirrors the API base URL above)
VITE_API_URL=https://api.ippan.org

# Build/runtime settings for the optional Node.js static server
NODE_ENV=production
PORT=4000
