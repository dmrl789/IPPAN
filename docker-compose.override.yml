version: '3.8'
services:
  ippan-node:
    build:
      dockerfile: Dockerfile.runtime-no-frontend
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s


