# IPPAN Deployment Environment Configuration
# Copy this file to .env and fill in your specific values
# DO NOT commit .env to version control!

# ============================================================================
# Network Configuration
# ============================================================================

# Network name displayed in UIs and logs
NETWORK_NAME=IPPAN Production

# Chain identifier
CHAIN_ID=ippan-mainnet

# ============================================================================
# Service Ports
# ============================================================================

# Unified UI port
UI_PORT=3001

# RPC API port
RPC_PORT=8080

# P2P network port
P2P_PORT=4001

# API Gateway port
GATEWAY_PORT=8081

# ============================================================================
# Node Identities (Multi-Server Deployments)
# ============================================================================

# Primary node (Server 1) hostname or IP
NODE_1_HOST=188.245.97.41

# Secondary node (Server 2) hostname or IP
NODE_2_HOST=135.181.145.174

# ============================================================================
# Docker Image Configuration
# ============================================================================

# Docker image tags (optional - defaults to latest)
# Use specific version tags for production deployments

# Unified UI image tag
IPPAN_UI_TAG=latest

# Node image tag
IPPAN_NODE_TAG=latest

# Gateway image tag
IPPAN_GATEWAY_TAG=latest

# Global image tag (used by docker-compose.production.yml)
IMAGE_TAG=latest

# ============================================================================
# Deployment Configuration
# ============================================================================

# Application deployment directory on remote servers
DEPLOY_APP_DIR=/opt/ippan

# ============================================================================
# SSL/TLS Configuration (Optional but Recommended for Production)
# ============================================================================

# SSL certificate path
SSL_CERT_PATH=/etc/ssl/certs/ssl-cert-snakeoil.pem

# SSL private key path
SSL_KEY_PATH=/etc/ssl/private/ssl-cert-snakeoil.key

# ============================================================================
# Gateway-Specific Variables (Optional Overrides)
# ============================================================================

# Gateway host port mapping (for docker-compose)
# GATEWAY_HOST_IP=127.0.0.1
# GATEWAY_HOST_PORT=8081

# UI host port mapping (for docker-compose)
# UI_HOST_PORT=3001

# ============================================================================
# Production Example
# ============================================================================

# NETWORK_NAME=IPPAN Mainnet
# CHAIN_ID=ippan-mainnet
# NODE_1_HOST=node1.ippan.network
# NODE_2_HOST=node2.ippan.network
# IPPAN_UI_TAG=v1.0.0
# IPPAN_NODE_TAG=v1.0.0
# IPPAN_GATEWAY_TAG=v1.0.0
# SSL_CERT_PATH=/etc/letsencrypt/live/ippan.network/fullchain.pem
# SSL_KEY_PATH=/etc/letsencrypt/live/ippan.network/privkey.pem

# ============================================================================
# Staging Example
# ============================================================================

# NETWORK_NAME=IPPAN Staging
# CHAIN_ID=ippan-staging
# NODE_1_HOST=staging1.ippan.network
# NODE_2_HOST=staging2.ippan.network
# IPPAN_UI_TAG=staging
# IPPAN_NODE_TAG=staging
# IPPAN_GATEWAY_TAG=staging

# ============================================================================
# Local Development Example
# ============================================================================

# NETWORK_NAME=IPPAN Local
# CHAIN_ID=ippan-local
# NODE_1_HOST=localhost
# NODE_2_HOST=localhost
# IPPAN_UI_TAG=dev
# IPPAN_NODE_TAG=dev
# IPPAN_GATEWAY_TAG=dev

# ============================================================================
# Notes
# ============================================================================

# 1. Use specific version tags (not 'latest') for production
# 2. Ensure SSL certificates are valid and not self-signed in production
# 3. NODE_*_HOST should be publicly accessible IPs or domains
# 4. Ports should not conflict with other services
# 5. DEPLOY_APP_DIR should have proper permissions
# 6. For multi-server setups, ensure nodes can communicate on P2P_PORT
# 7. Consider using a secrets manager for sensitive SSL keys